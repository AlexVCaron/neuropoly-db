# /etc/nginx/conf.d/cache.conf

# Define the GitHub token cache zone
proxy_cache_path /var/cache/nginx/github_tokens 
                    levels=1:2 
                    keys_zone=token_cache:10m 
                    max_size=1g 
                    inactive=60m 
                    use_temp_path=off;
